// Deprecated: this component's logic now lives in MediaCard.jsx (variant="tiktok"),
// which also handles the youtube/podcast/placeholder variants used across the site.
// Kept as a thin re-export so any stale imports don't break the build.
export { default } from './MediaCard';
